function stackHeight3d(layers) {
    // take the hight of one ball 
    // figure out the how low a ball is going to have to sit on a ball and subtact that amount from the hight
    return  layers / Math.SQRT2
}